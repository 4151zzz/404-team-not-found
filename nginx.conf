events {
    worker_connections 1024;
}

http {
    upstream strapi {
        server 404-team-application:1337;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://strapi;
        }
    }
}